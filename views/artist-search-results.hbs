{{!-- <h1> {{this.items}} </h1> --}}

{{#each this.items}}
<ul>
    <li> {{name}} </li>
    <img src={{images.0.url}} alt="artist image" width="200px">
    <a href="/albums/{{id}}">View Albums</a>
    {{!-- accessing indices in hbs is . notation --}}
</ul>
{{/each}}
